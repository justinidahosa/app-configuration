apiVersion: v1
kind: Secret
metadata:
  name: backend-db-secret
  namespace: backend
type: Opaque
stringData:
  # These are placeholders. The CSI driver will overwrite these 
  # with real AWS values as soon as the Pod starts.
  DB_HOST: "pending"
  DB_USER: "pending"
  DB_PASSWORD: "pending"
  DB_NAME: "pending"
  DB_PORT: "pending"