apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: frontend
  labels:
    app: frontend
spec:
  selector:
    app: frontend
    version: v2
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080