apiVersion: v1
kind: Service
metadata:
  name: frontend-v1-service
  namespace: frontend
spec:
  selector:
    app: frontend
    version: v1
  ports:
    - name: TCP
      port: 80
      targetPort: 8080