apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-v2
  namespace: backend
  annotations: 
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
      version: v2
  template:
    metadata:
      labels:
        app: backend
        version: v2
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: backend-sa
      containers:
        - name: backend
          image: 284432073095.dkr.ecr.us-east-1.amazonaws.com/backend-v2:18be8be9af57013f21b5ed7055a1c826cb2f052f
          imagePullPolicy: Always
          ports:
            - containerPort: 8000

          volumeMounts:
            - name: secrets-store
              mountPath: "/mnt/secrets-store"
              readOnly: true

          env:
            # App config
            - name: DEPLOYMENT
              valueFrom:
                configMapKeyRef:
                  name: backend-v2-config
                  key: DEPLOYMENT

            - name: VERSION
              valueFrom:
                configMapKeyRef:
                  name: backend-v2-config
                  key: VERSION

          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3

      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: backend-db-secrets