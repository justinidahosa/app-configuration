apiVersion: v1
kind: Service
metadata:
  name: backend-v2-service
  namespace: backend
spec:
  selector:
    app: backend
    version: v2
  ports:
    - name: http
      port: 80
      targetPort: 8000